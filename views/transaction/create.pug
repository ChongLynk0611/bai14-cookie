extends ../layout/layout.pug 
block content
    h1 create transaction
    if errors  
        each error in errors    
            .alert.alert-danger.ml-4=error
    form(action="/transaction" , method= "POST")
        .form-group.ml-4
            
            label id_Book
            input(type="text" ,name="idBook" , value =values ? values.idBook :"").form-control
            label id_user
            input(type="text", name="idUser",value=values ? values.idUser:"").form-control
            button.btn.btn-primary.mt-2 submit